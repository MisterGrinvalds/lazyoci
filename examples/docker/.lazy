version: 1
artifacts:
  - type: docker
    name: local-nginx
    image: nginx:alpine
    targets:
      - registry: "{{ .Registry }}/examples/nginx-mirror"
        tags:
          - "{{ .Version }}"
          - latest
