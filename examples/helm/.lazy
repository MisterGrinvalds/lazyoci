version: 1
artifacts:
  - type: helm
    name: mychart
    chartPath: mychart
    targets:
      - registry: localhost:5050/examples/charts/mychart
        tags:
          - "{{ .ChartVersion }}"
          - latest
