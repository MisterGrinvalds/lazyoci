version: 1
artifacts:
  - type: helm
    name: mychart
    chartPath: mychart
    targets:
      - registry: "{{ .Registry }}/examples/charts/mychart"
        tags:
          - "{{ .ChartVersion }}"
          - latest
