version: 1
artifacts:
  - type: image
    name: hello-server
    dockerfile: Dockerfile
    context: "."
    platforms:
      - linux/amd64
      - linux/arm64
    buildArgs:
      GO_VERSION: "1.23"
    targets:
      - registry: "{{ .Registry }}/examples/hello-server"
        tags:
          - "{{ .Version }}"
          - "{{ .VersionMajorMinor }}"
          - "{{ .GitSHA }}"
          - latest
